package com.polopoly.ps.pcmd.tool;

/**
 * Marker interface for a tool that does not need Polopoly (it will not get a
 * context) and not have default polopoly client options (server, loginuser,
 * etc).
 */
public interface DoesNotRequireRunningPolopoly {

}
